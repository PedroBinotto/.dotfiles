return {
 settings = {
    html = {
      format = {
        -- Set the token to define unformatted content
        unformatted = "b,code,pre,textarea,script,style", -- Add or modify as needed
        unformattedContentDelimiter = "noformat",
      },
    },
  }
}
